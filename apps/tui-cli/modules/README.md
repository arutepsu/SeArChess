# TUI CLI Modules

These modules are owned by the local text UI app.

| Module | Role |
|---|---|
| `tui` | Text UI adapter and console runner support. |

The TUI is a local client/runtime, not part of the Game Service process.
