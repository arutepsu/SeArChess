# SeArChess Web UI

## Setup

```bash
npm install
```

## Run

```bash
npm run dev
```

## Configuration

- `VITE_API_BASE_URL` (default: `http://localhost:8080`)
- `VITE_API_MOCK` set to `true` to use mock data
