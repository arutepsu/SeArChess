export interface FrontendSessionContext {
  sessionId: string;
  gameId: string;
  mode: string;
  lifecycle: string;
  whiteController: string;
  blackController: string;
  createdAt: string;
  updatedAt: string;
}