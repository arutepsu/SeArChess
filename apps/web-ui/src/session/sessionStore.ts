export type SessionContext = {
  sessionId: string;
  gameId: string;
  mode: string;
  lifecycle: string;
  whiteController: string;
  blackController: string;
  createdAt: string;
  updatedAt: string;
};
