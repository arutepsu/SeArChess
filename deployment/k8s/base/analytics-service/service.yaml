apiVersion: v1
kind: Service
metadata:
  name: analytics-service
  namespace: searchess
  labels:
    app: analytics-service
spec:
  type: ClusterIP
  selector:
    app: analytics-service
  ports:
    - name: http
      port: 8084
      targetPort: http
