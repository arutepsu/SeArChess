apiVersion: v1
kind: Service
metadata:
  name: game-service
  namespace: searchess
  labels:
    app: game-service
spec:
  type: ClusterIP
  selector:
    app: game-service
  ports:
    - name: http
      port: 8080
      targetPort: http
    - name: ws
      port: 9090
      targetPort: ws
