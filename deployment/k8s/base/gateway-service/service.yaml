apiVersion: v1
kind: Service
metadata:
  name: gateway-service
  namespace: searchess
  labels:
    app: gateway-service
spec:
  type: ClusterIP
  selector:
    app: gateway-service
  ports:
    - name: http
      port: 8087
      targetPort: http
