# Dev-only secret values for uni-server-registry.
# These are NOT production secrets — they mirror the Compose dev defaults.
# Do NOT use this overlay in any internet-accessible environment.
apiVersion: v1
kind: Secret
metadata:
  name: searchess-secrets
  namespace: searchess
stringData:
  postgres-password: "searchess"
  grafana-admin-password: "admin"
  migration-admin-token: ""
  external-game-bot-api-key: ""
  lichess-bot-token: ""
